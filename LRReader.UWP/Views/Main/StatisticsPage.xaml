<Page
	x:Class="LRReader.Views.Main.StatisticsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:charting="using:WinRTXamlToolkit.Controls.DataVisualization.Charting"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:LRReader.Views.Main"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	DataContext="{Binding StatisticsPageInstance, Source={StaticResource Locator}}"
	NavigationCacheMode="Enabled"
	mc:Ignorable="d">

	<Grid>
		<ScrollViewer VerticalScrollBarVisibility="Auto">
			<StackPanel>
				<charting:Chart>
					<charting:PieSeries DependentValuePath="weight" IndependentValuePath="text" ItemsSource="{x:Bind Data.TagStats}" />
				</charting:Chart>
				<charting:Chart Height="400">
					<charting:ColumnSeries
						Title="Tags" DependentValuePath="weight" IndependentValuePath="text"
						ItemsSource="{x:Bind Data.TagStats}" />
				</charting:Chart>
			</StackPanel>
		</ScrollViewer>
		<ProgressRing Width="80" Height="80" IsActive="{x:Bind Data.LoadingStats, Mode=OneWay}" />
		<Button HorizontalAlignment="Center" Click="Button_Click" Visibility="{x:Bind Data.RefreshOnErrorButton, Mode=OneWay}">Retry</Button>
	</Grid>
</Page>
