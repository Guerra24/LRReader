<Page
	x:Class="LRReader.Views.Main.ReaderPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:items="using:LRReader.Views.Items"
	xmlns:local="using:LRReader.Views.Main"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	DataContext="{Binding ReaderPageInstance, Source={StaticResource Locator}}"
	NavigationCacheMode="Enabled"
	mc:Ignorable="d">

	<Grid>
		<FlipView
			Name="FlipView" Background="Transparent" FocusVisualPrimaryThickness="0"
			FocusVisualSecondaryThickness="0"
			ItemsSource="{Binding ArchiveImages}"
			Loaded="FlipView_Loaded" UseTouchAnimationsForAllNavigation="True" Tapped="FlipView_Tapped">
			<FlipView.ItemTemplate>
				<DataTemplate>
					<items:ReaderImage />
				</DataTemplate>
			</FlipView.ItemTemplate>
		</FlipView>
	</Grid>
</Page>
