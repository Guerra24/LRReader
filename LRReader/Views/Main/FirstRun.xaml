<Page
	x:Class="LRReader.Views.Main.FirstRun"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:LRReader.Views.Main"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	DataContext="{Binding SettingsPageInstance, Source={StaticResource Locator}}"
	Unloaded="Page_Unloaded"
	mc:Ignorable="d">

	<Grid>
		<StackPanel Padding="10" Width="300" HorizontalAlignment="Center" VerticalAlignment="Center">
			<TextBlock
				Margin="0,0,0,10" HorizontalAlignment="Center" FontSize="28"
				Text="Welcome" />
			<TextBox
				Name="Server" Grid.Row="1" Margin="0,0,0,10"
				PlaceholderText="Server Address"
				Text="{Binding SettingsManager.ServerAddress, Mode=TwoWay}" />
			<PasswordBox
				Name="ApiKey" Grid.Row="2" Margin="0,0,0,10"
				Password="{Binding SettingsManager.ServerApiKey, Mode=TwoWay}"
				PlaceholderText="API Key" />
			<Button Name="AcceptBtn" HorizontalAlignment="Center" Click="AcceptBtn_Click">Accept</Button>
		</StackPanel>
	</Grid>
</Page>
