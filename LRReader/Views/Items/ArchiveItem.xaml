<UserControl
	x:Class="LRReader.Views.Items.ArchiveItem"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:LRReader.Views.Items"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Width="200"
	d:DesignHeight="400"
	d:DesignWidth="200"
	DataContextChanged="UserControl_DataContextChanged"
	mc:Ignorable="d">

	<Grid Padding="0" Background="{ThemeResource BrushItemGridBackground}" ToolTipService.ToolTip="{x:Bind Archive.title}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock
			Name="Title" Grid.Row="0" HorizontalAlignment="Center"
			FontSize="16"
			Text="{x:Bind Archive.title}"
			TextTrimming="CharacterEllipsis" />
		<Grid Grid.Row="1" Height="282" BorderBrush="{ThemeResource SystemAccentColor}">
			<Image Name="Thumbnail" VerticalAlignment="Center" Stretch="UniformToFill" />
			<ProgressRing
				Name="Ring" Width="50" Height="50"
				IsActive="True" />
		</Grid>
	</Grid>
</UserControl>
